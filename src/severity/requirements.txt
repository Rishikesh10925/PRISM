torch>=2.6.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
timm>=1.0.0
scipy>=1.11.0
pytest>=8.0.0

# torch above is a CPU wheel by default; for GPU install the CUDA build matching your
# driver instead, e.g. pip install torch --index-url https://download.pytorch.org/whl/cu130
